# deploy

Прод-артефакты (docker-compose для VPS, Caddyfile, `push_data.sh`) добавляются в Этап 6 — см. `docs/DEPLOY.md`.

Локальная разработка использует `docker-compose.yml` в корне репозитория, не этот каталог.
